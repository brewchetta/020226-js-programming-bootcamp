// FUNCTION SCOPE EXERCISES

// 1. Define a changeable variable `currentTime` in the global scope.

// 2. Define a function `updateCurrentTime` which gets the current hour and minute and changes the currentTime variable in global scope so that its value is: "{hour}:{minute}", for example "10:52". 

// You can get the current hour and minute with: 
// const time = new Date() 
// const hour = time.getHours()
// const minute = time.getMinutes() 